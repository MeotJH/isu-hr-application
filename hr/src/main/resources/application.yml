spring:
  application:
    name: hr
  profiles:
    active: local  # ???
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true


# Server ??
server:
  port: 8080
  servlet:
    context-path: /api